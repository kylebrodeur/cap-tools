# Normalize line endings to LF in the repo (this project spans Windows + WSL).
* text=auto eol=lf

# Treat these as binary (no EOL conversion, no diff).
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.mp4  binary
*.mov  binary
*.ogg  binary
*.mp3  binary
